import * as React from 'react';
import Root from 'containers/Root';
import './App.scss';

class App extends React.Component {
  render() {
    return (
      <Root />
    );
  }
}

export default App;
